var event = new KeyboardEvent('keydown', {
	key: 'g',
	ctrlKey: true
});

setInterval(function(){
	for (i = 0; i < 100; i++) {
		document.dispatchEvent(event);
	}
}, 0);


go to popcat, inspect, console, copy and paste lines 1 through 10, click "ENTER". popcat goes crazy. leave computer on and enjoy the score
